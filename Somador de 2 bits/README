O TestBench percorre todas as combinações possíveis dos valores de entrada (a e b), e para cada combinação, a soma é calculada e a saída é registrada.
É possível usar as ferramentas de simulação para verificar o comportamento correto do somador
